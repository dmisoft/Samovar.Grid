namespace Samovar.Blazor
{
    public enum DataSourceState
    {
        Idle,
        Loading,
        NoData
    }

    public enum DataEditState
    {
        Idle,
        Editing,
        Inserting
    }
}
