namespace Samovar.Grid
{
    public enum DataSourceState
    {
        Idle,
        Loading,
        NoData
    }

    public enum DataEditState
    {
        Idle,
        Editing,
        Inserting
    }
}
